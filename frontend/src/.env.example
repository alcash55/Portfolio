# Copy to frontend/src/.env (Vite is configured with envDir: './src').
# Public value - anything VITE_-prefixed is inlined into the client bundle,
# so never put a secret here.
VITE_API_URL=http://localhost:8080
